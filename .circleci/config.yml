version: 2.1

orbs:
  linter: talkiq/linter@2
  poetry: talkiq/poetry@2

workflows:
  run-jobs:
    jobs:
      - linter/pre-commit
      - poetry/run: &pytest
          name: pytest
          cmd: pytest
          python_version: 3.10.4
