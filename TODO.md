# TODO

- [ ] Move custom JQL configuration from environment variable to in-app Settings UI.
- [ ] Persist sync configuration in the database (instead of env-only runtime config).
- [ ] Support editing and validation of custom JQL without application restart.
